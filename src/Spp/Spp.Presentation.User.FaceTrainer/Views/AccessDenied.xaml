<Page x:Name="AccessDeniedPage"
    x:Class="FaceAPITrainer.Views.AccessDenied"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="120" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="1"  x:Name="accessDeniedGrid">
            <TextBlock x:Name="textBlock" Text="Access Denied. Please, contact the Administrator." TextAlignment="Center" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="32" Foreground="{ThemeResource SystemControlForegroundAccentBrush}" FontStyle="Normal" FontWeight="Bold" Opacity="100" />
        </Grid>
        <Grid Grid.Row="2">
            <Button x:Name="BackButton" HorizontalAlignment="Center" VerticalAlignment="Top" Tapped="BackButton_OnTapped" 
                    Height="60" Width="180"
                    Margin="0"
                    FontSize="20"
                    Padding="0" Opacity="0.8">Go Back</Button>
        </Grid>

    </Grid>


</Page>
